apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "easyedit.name" . }}-test-connection"
  labels:
    {{- include "easyedit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['-qO-', 'http://{{ include "easyedit.name" . }}:{{ .Values.service.port }}/']
  restartPolicy: Never
